const React = require('react');
const Default = require('./Default');

class Show extends React.Component {
    render() {


        return (
            <Default>
                <h1>Show Heading</h1>
            </Default>

        )
    }
}

module.exports = Show;